


import 'package:flutter/material.dart';

class ColorTheme {
  // primary and secondary colors of components
  // ff at the beginning means opacity
  static const Color primary = Color(0xff8e24aa);
  static const Color secondary = Color(0xffd81b60);

  // light adn dark colors of components
  static const Color primaryLight = Color(0xffc158dc);
  static const Color primaryDark = Color(0xff5c007a);
  static const Color secondaryLight = Color(0xffa00037);
  static const Color secondaryDark = Color(0xffff5c8d);

  // text colors on components
  static const Color onPrimary = Color(0xffffffff);
  static const Color onSecondary = Color(0xffffffff);
  static const Color onLight = Color(0xff000000);
  static const Color onDark = Color(0xffffffff);

  // surfaces
  static const Color background = Color(0xffe1e2e1);
  static const Color surface = Color(0xffffffff);
  static const Color error = Color(0xffb00020);

  // text colors on surfaces
  static const Color onBackground = Color(0xff000000);
  static const Color onSurface = Color(0xff000000);
  static const Color onError = Color(0xffffffff);

}


